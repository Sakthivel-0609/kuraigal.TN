Django>=4.2,<5.0
Pillow>=10.0
reportlab>=4.0
qrcode>=7.4

# --- Production deployment (Render / Railway / any real host) ---
gunicorn>=21.2          # production WSGI server (replaces `runserver`)
whitenoise>=6.6          # serves static files without a separate web server
psycopg2-binary>=2.9     # PostgreSQL driver, only used when DJANGO_DB_ENGINE=postgresql
